import {{pascalCase name}} from './{{pascalCase name}}.vue';

export default {
  title: 'Components/UI Elements/{{pascalCase name}}',
  component: {{pascalCase name}},
  argTypes: {
    demo: {
      control: {
        type: 'select',
        options: ['1', '2', '3', '4'],
      },
      defaultValue: '1',
    },
  },

  parameters: {
    componentSubtitle: '',
  },
};

const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { {{pascalCase name}} },
  template: '<{{pascalCase name}} v-bind="$props">{{label}}</{{pascalCase name}}>',
});

export const Default = Template.bind({});
Default.args = {};
